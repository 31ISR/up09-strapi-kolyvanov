info:
  name: Students
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}api/articles/:id?populate=*"
  params:
    - name: populate
      value: "*"
      type: query
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
